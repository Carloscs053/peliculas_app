name: presentacion_t5
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_card_swipper: ^0.4.0
  go_router: ^17.0.1
  google_fonts: ^8.1.0
  http: ^1.6.0
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  url_strategy: ^0.3.0
  youtube_player_iframe: ^5.2.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
